// user functions
import time

function getFeedTiming(feedLocation: number, startTime: number, endTime: number, interval: number) : string {
    // user programs how long and when the animals are fed, placeholder parameters because we dont know when theyre fed
    const resultString = "we are feeding farm ${feedLocation}, within ${startTime} and ${endTime}";
    return resultString;
}

function gateToggle (gateNum: number) : number {
    // toggles a specific gate if its open we close it vice versa
    return 0;
}

function togglePest (machineNum: number): number {
    // toggles a pest control mechanism
    return 0;
}
